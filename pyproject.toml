[project]
name = "pmark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
test = [
    "markdown-it-py>=4.2.0",
    "pytest>=9.0.3",
    "requests>=2.34.2",
]
